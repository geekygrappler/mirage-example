<h2>User</h2>

Name: {{model.name}}

{{#each model.things as |thing|}}
  <p>Thing: {{thing.title}}</p>
{{/each}}

{{#each model.specialThings as |thing|}}
  <p>Special Thing: {{thing.title}}</p>
{{/each}}

{{outlet}}